function authors(req, res) {
  //crud operations
  //create, read, update, delete
  //POST, GET, PUT, DELETE
  //POST  authors
  if (req.method === 'POST') {
    //create a author
  }
  //GET  authors
  if (req.method === 'GET') {
    //get all authors
  }
  //GET  authors/:id
  if (req.method === 'GET') {
    //get a author by id
  }
  //PUT  authors/:id
  if (req.method === 'PUT') {
    //get a author by id
  }
  //DELETE  authors/:id
  if (req.method === 'DELETE') {
    //delete author by id
  }
  res.send('authors API');
}
